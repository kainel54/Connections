using HB.Skill;
using UnityEngine;

public class TriggerCollider : MonoBehaviour
{
    // _sehyun : i change the Stomp to RangeStompObj. 
    /* 
    private RangeStomp _stomp;

    void Start()
    {
        _stomp = GetComponentInParent<RangeStomp>();
        
        Destroy(gameObject, 0.1f);
    }

    void OnTriggerEnter(Collider other)
    {
        _stomp.ApplyDamage(other);
    }
    */
}
